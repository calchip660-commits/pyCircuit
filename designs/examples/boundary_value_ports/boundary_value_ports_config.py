from __future__ import annotations

DEFAULT_PARAMS = {
    "width": 32,
}

TB_PRESETS = {
    "smoke": {"timeout": 64, "finish": 1},
    "nightly": {"timeout": 128, "finish": 4},
}

SIM_TIER = "normal"
